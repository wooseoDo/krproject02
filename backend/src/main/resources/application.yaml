spring:
  application:
    name: krproject02
  datasource:
    url: jdbc:postgresql://localhost:45432/postgres
    username: postgres
    password: 4068
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: never